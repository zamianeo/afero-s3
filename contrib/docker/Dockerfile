ARG VERSION=RELEASE.2022-04-09T15-09-52Z
ARG TARGETPLATFORM=linux/amd64
ARG TARGETOS=linux
ARG TARGETARCH=amd64
FROM --platform=${TARGETPLATFORM} minio/minio:${VERSION}

ENTRYPOINT [ "/bin/sh", "-c", "minio ${MINIO_COMMAND:-server} ${MINIO_FLAGS} ${MINIO_ARGS:-/data}"]